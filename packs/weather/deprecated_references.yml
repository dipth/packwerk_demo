# This file contains a list of dependencies that are not part of the long term plan for packs/weather.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packs/weather
---
".":
  "::ApplicationController":
    violations:
    - dependency
    files:
    - packs/weather/app/controllers/weather_controller.rb
